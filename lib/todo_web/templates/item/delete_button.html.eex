<%=
form_for @conn, list_item_path(@conn, :delete, @item.list, @item),
[
  method: :delete,
  as: :delete_params,
  id: "item-delete-from",
  onsubmit: delete_confirmation(@item.name),
],
fn _ ->%>
  <button class="btn btn-danger">Delete</button>
<% end %>
